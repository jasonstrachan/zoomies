<Window x:Class="Zoomies.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        mc:Ignorable="d"
        Title="Zoomies" 
        Height="1" 
        Width="1"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        Left="-100"
        Top="-100">
    <Grid>
        <!-- System Tray Icon -->
        <tb:TaskbarIcon x:Name="TrayIcon"
                        IconSource="pack://application:,,,/assets/images/thumb2.ico"
                        ToolTipText="💀 Zoomies - Screen Magnifier (Ctrl+Alt+Scroll)">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="About" Click="OnAboutClick"/>
                    <Separator/>
                    <MenuItem Header="Start with Windows" 
                              x:Name="StartWithWindowsMenuItem"
                              IsCheckable="True"
                              Click="OnStartWithWindowsClick"/>
                    <Separator/>
                    <MenuItem Header="Exit" Click="OnExitClick"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Grid>
</Window>